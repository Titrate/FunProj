<h1>Listing sys_cleans</h1>

<table>
  <tr>
    <th>Trash bin contents</th>
    <th>New packages</th>
    <th>Sys op</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sys_cleans.each do |sys_clean| %>
  <tr>
    <td><%= sys_clean.trash_bin_contents %></td>
    <td><%= sys_clean.new_packages %></td>
    <td><%= sys_clean.sys_op_id %></td>
    <td><%= link_to 'Show', sys_clean %></td>
    <td><%= link_to 'Edit', edit_sys_clean_path(sys_clean) %></td>
    <td><%= link_to 'Destroy', sys_clean, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sys clean', new_sys_clean_path %>
